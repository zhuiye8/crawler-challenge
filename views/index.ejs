<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>扬州大自然 AI能力应用测试 - Web Scraping Challenge</title>
  <style>
    * { box-sizing: border-box; margin: 0; padding: 0; }
    body {
      font-family: 'Microsoft YaHei', 'PingFang SC', sans-serif;
      background: linear-gradient(135deg, #0f0c29 0%, #302b63 50%, #24243e 100%);
      min-height: 100vh;
      color: #fff;
    }
    .hero {
      text-align: center;
      padding: 80px 20px 60px;
    }
    .hero h1 {
      font-size: 3.5em;
      margin-bottom: 20px;
      background: linear-gradient(90deg, #667eea, #764ba2, #f5af19);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      animation: gradient 3s ease infinite;
      background-size: 200% auto;
    }
    @keyframes gradient {
      0% { background-position: 0% center; }
      50% { background-position: 100% center; }
      100% { background-position: 0% center; }
    }
    .hero p {
      font-size: 1.3em;
      color: rgba(255,255,255,0.8);
      max-width: 600px;
      margin: 0 auto 30px;
    }
    .timer-box {
      background: rgba(255,255,255,0.1);
      display: inline-block;
      padding: 15px 40px;
      border-radius: 10px;
      font-size: 1.5em;
      font-family: monospace;
    }
    .container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 0 20px;
    }
    .levels-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: 20px;
      margin-bottom: 40px;
    }
    .level-card {
      background: rgba(255,255,255,0.05);
      border-radius: 15px;
      padding: 25px;
      border: 1px solid rgba(255,255,255,0.1);
      transition: transform 0.3s, box-shadow 0.3s;
      text-decoration: none;
      color: inherit;
      display: block;
    }
    .level-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 10px 30px rgba(0,0,0,0.3);
      border-color: rgba(102, 126, 234, 0.5);
    }
    .level-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 15px;
    }
    .level-number {
      font-size: 2em;
      font-weight: bold;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
    }
    .level-points {
      background: rgba(46, 204, 113, 0.2);
      color: #2ecc71;
      padding: 5px 12px;
      border-radius: 15px;
      font-size: 0.9em;
      font-weight: 600;
    }
    .level-title {
      font-size: 1.2em;
      margin-bottom: 10px;
    }
    .level-desc {
      color: rgba(255,255,255,0.6);
      font-size: 0.95em;
      line-height: 1.5;
    }
    .level-tags {
      margin-top: 15px;
      display: flex;
      flex-wrap: wrap;
      gap: 8px;
    }
    .tag {
      background: rgba(255,255,255,0.1);
      padding: 4px 10px;
      border-radius: 5px;
      font-size: 0.8em;
    }
    .info-section {
      background: rgba(255,255,255,0.05);
      border-radius: 15px;
      padding: 30px;
      margin-bottom: 30px;
    }
    .info-section h2 {
      margin-bottom: 20px;
      color: #667eea;
    }
    .info-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 20px;
    }
    .info-item {
      background: rgba(0,0,0,0.2);
      padding: 20px;
      border-radius: 10px;
    }
    .info-item h3 {
      margin-bottom: 10px;
      font-size: 1.1em;
    }
    .info-item p, .info-item ul {
      color: rgba(255,255,255,0.7);
      font-size: 0.95em;
    }
    .info-item ul {
      list-style: none;
      padding: 0;
    }
    .info-item li {
      padding: 5px 0;
      padding-left: 20px;
      position: relative;
    }
    .info-item li::before {
      content: '✓';
      position: absolute;
      left: 0;
      color: #2ecc71;
    }
    .cta-buttons {
      display: flex;
      gap: 15px;
      justify-content: center;
      margin: 40px 0;
      flex-wrap: wrap;
    }
    .btn {
      padding: 15px 30px;
      border-radius: 10px;
      font-size: 1.1em;
      text-decoration: none;
      transition: transform 0.2s, box-shadow 0.2s;
    }
    .btn-primary {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: #fff;
    }
    .btn-secondary {
      background: rgba(255,255,255,0.1);
      color: #fff;
      border: 1px solid rgba(255,255,255,0.2);
    }
    .btn:hover {
      transform: translateY(-2px);
      box-shadow: 0 5px 20px rgba(0,0,0,0.3);
    }
    .api-docs {
      background: rgba(0,0,0,0.3);
      padding: 20px;
      border-radius: 10px;
      margin-top: 20px;
    }
    .api-docs h3 {
      margin-bottom: 15px;
    }
    .api-endpoint {
      background: rgba(0,0,0,0.3);
      padding: 10px 15px;
      border-radius: 5px;
      margin-bottom: 10px;
      font-family: monospace;
      font-size: 0.9em;
    }
    .method {
      color: #2ecc71;
      font-weight: bold;
    }
    .footer {
      text-align: center;
      padding: 30px;
      color: rgba(255,255,255,0.5);
      font-size: 0.9em;
    }
  </style>
</head>
<body>
  <div class="hero">
    <h1>🤖 扬州大自然 AI能力应用测试</h1>
    <p>测试你借助AI工具完成编程任务的能力！挑战逐级递增的难度关卡，使用 Claude Code / Cursor / Copilot 等AI助手，逆向分析 API、绕过反爬虫机制！</p>
  </div>

  <div class="container">
    <!-- Registration Section - Moved to Top -->
    <div class="info-section" style="margin-bottom: 40px;">
      <h2>📝 考生注册</h2>

      <!-- 重要提示 -->
      <div style="background: rgba(241, 196, 15, 0.15); border: 2px solid #f1c40f; padding: 15px 20px; border-radius: 10px; margin-bottom: 20px;">
        <p style="color: rgba(255,255,255,0.9); margin: 0; font-size: 0.95em;">
          ⚠️ <strong style="color: #f39c12;">重要：</strong>Level 3（登录+反爬虫）需要使用你的考试ID登录，请务必先完成注册！
        </p>
      </div>

      <!-- Registration Status Display (shown when already registered) -->
      <div id="registrationStatus" style="display: none;"></div>

      <!-- Registration Form (shown when not registered) -->
      <div id="registrationForm">
        <p style="color: rgba(255,255,255,0.7); margin-bottom: 20px; text-align: center;">
          注册后将获得唯一的考试ID，用于提交答案。<strong style="color: #f1c40f;">完成 Level 3 时，用户名为你的考试ID，密码为 test123</strong>
        </p>
        <div style="max-width: 500px; margin: 0 auto;">
          <div style="display: flex; gap: 10px; margin-bottom: 15px;">
            <input
              type="text"
              id="candidateName"
              placeholder="请输入你的姓名（2-20个字符）"
              style="flex: 1; padding: 12px 20px; border-radius: 8px; border: 1px solid rgba(255,255,255,0.2); background: rgba(0,0,0,0.3); color: #fff; font-size: 1em;"
              maxlength="20"
            />
            <button
              onclick="registerCandidate()"
              style="padding: 12px 30px; border-radius: 8px; border: none; background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); color: #fff; font-size: 1em; cursor: pointer; white-space: nowrap;"
            >
              注册
            </button>
          </div>
          <div id="registrationResult" style="padding: 15px; border-radius: 8px; display: none;"></div>
        </div>
      </div>
    </div>

    <div class="levels-grid">
      <a href="/level1" class="level-card">
        <div class="level-header">
          <span class="level-number">第1关</span>
          <span class="level-points">15 分</span>
        </div>
        <h3 class="level-title">静态 HTML 页面</h3>
        <p class="level-desc">
          从简单的 HTML 表格中爬取商品数据。
          小心隐藏的陷阱！
        </p>
        <div class="level-tags">
          <span class="tag">BeautifulSoup</span>
          <span class="tag">Requests</span>
          <span class="tag">蜜罐陷阱</span>
        </div>
      </a>

      <a href="/level2" class="level-card">
        <div class="level-header">
          <span class="level-number">第2关</span>
          <span class="level-points">25 分</span>
        </div>
        <h3 class="level-title">分页 + AJAX 加载</h3>
        <p class="level-desc">
          商品分布在 10 个分页，价格通过 API 异步加载。
          注意频率限制！
        </p>
        <div class="level-tags">
          <span class="tag">API逆向</span>
          <span class="tag">分页处理</span>
          <span class="tag">频率限制</span>
        </div>
      </a>

      <a href="/level3/login" class="level-card">
        <div class="level-header">
          <span class="level-number">第3关</span>
          <span class="level-points">25 分</span>
        </div>
        <h3 class="level-title">登录 + 请求头检测</h3>
        <p class="level-desc">
          需要登录才能访问。处理 CSRF 令牌和 UA 检测。
          过滤掉假数据！
        </p>
        <div class="level-tags">
          <span class="tag">会话管理</span>
          <span class="tag">CSRF</span>
          <span class="tag">User-Agent</span>
        </div>
      </a>

      <a href="/level4" class="level-card">
        <div class="level-header">
          <span class="level-number">第4关</span>
          <span class="level-points">35 分</span>
        </div>
        <h3 class="level-title">机器人检测 + 验证码</h3>
        <p class="level-desc">
          浏览器指纹检测和滑块验证码。
          需要使用无头浏览器的隐身模式！
        </p>
        <div class="level-tags">
          <span class="tag">Playwright</span>
          <span class="tag">浏览器指纹</span>
          <span class="tag">验证码</span>
        </div>
      </a>
    </div>

    <div class="cta-buttons">
      <a href="/scoreboard" class="btn btn-primary">🏆 实时排行榜</a>
    </div>

    <div class="info-section">
      <h2>📋 规则说明</h2>
      <div class="info-grid">
        <div class="info-item">
          <h3>⏱️ 时间限制</h3>
          <p>60 分钟内完成全部 4 个关卡。排名依据总分和完成时间。</p>
        </div>
        <div class="info-item">
          <h3>🔧 允许使用的工具</h3>
          <ul>
            <li>Claude Code / Cursor / Copilot</li>
            <li>搜索引擎</li>
            <li>任意编程语言</li>
            <li>浏览器开发者工具</li>
          </ul>
        </div>
        <div class="info-item">
          <h3>⚠️ 提交限制</h3>
          <p>每个关卡最多提交 5 次，只计算最高分。</p>
        </div>
        <div class="info-item">
          <h3>🎯 评分标准</h3>
          <ul>
            <li>数据完整性 (30%)</li>
            <li>数据准确性 (40%)</li>
            <li>蜜罐规避 (20%)</li>
            <li>速度加分 (10%)</li>
          </ul>
        </div>
      </div>

      <div class="api-docs">
        <h3>📡 提交接口</h3>
        <div class="api-endpoint">
          <span class="method">POST</span> /api/submit
        </div>
        <pre style="background: rgba(0,0,0,0.3); padding: 15px; border-radius: 5px; overflow-x: auto;">
{
  "level": 1,
  "task_id": "你的考试ID",
  "data": [
    {"id": 1, "name": "商品1", "price": 99.99, "stock": 50, "sku": "ABC123"},
    ...
  ]
}</pre>
      </div>
    </div>
  </div>

  <div class="footer">
    <p>扬州大自然 AI能力应用测试平台 &copy; 2025</p>
    <p>测试考生借助AI工具（Claude Code、Cursor、Copilot等）解决实际编程问题的能力</p>
  </div>

  <script>
    // Check localStorage on page load
    window.addEventListener('load', function() {
      const savedData = localStorage.getItem('examData');
      if (savedData) {
        try {
          const examData = JSON.parse(savedData);
          showRegisteredStatus(examData);
        } catch (e) {
          console.error('Failed to parse saved exam data:', e);
          localStorage.removeItem('examData');
        }
      }
    });

    // Show registered status
    function showRegisteredStatus(data) {
      const statusDiv = document.getElementById('registrationStatus');
      const formDiv = document.getElementById('registrationForm');

      statusDiv.style.display = 'block';
      formDiv.style.display = 'none';

      statusDiv.innerHTML = `
        <div style="background: rgba(46, 204, 113, 0.2); border: 2px solid rgba(46, 204, 113, 0.5); padding: 25px; border-radius: 10px; max-width: 600px; margin: 0 auto;">
          <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 20px;">
            <span style="background: rgba(46, 204, 113, 0.3); color: #2ecc71; padding: 6px 15px; border-radius: 20px; font-weight: 600;">
              ✓ 已注册
            </span>
            <button onclick="clearRegistration()" style="background: transparent; border: 1px solid rgba(255,255,255,0.3); color: rgba(255,255,255,0.7); padding: 5px 12px; border-radius: 5px; cursor: pointer; font-size: 0.85em;">
              重新注册
            </button>
          </div>

          <div style="margin-bottom: 20px;">
            <p style="color: rgba(255,255,255,0.7); margin-bottom: 8px;">姓名</p>
            <p style="font-size: 1.2em; font-weight: 600;">${data.name}</p>
          </div>

          <div style="margin-bottom: 20px;">
            <p style="color: rgba(255,255,255,0.7); margin-bottom: 8px;">考试ID</p>
            <div style="display: flex; gap: 10px; align-items: center;">
              <code id="taskIdDisplay" style="background: rgba(0,0,0,0.3); padding: 8px 15px; border-radius: 6px; font-family: monospace; font-size: 1.1em; flex: 1;">${data.taskId}</code>
              <button onclick="copyTaskId('${data.taskId}')" style="background: rgba(52, 152, 219, 0.3); border: 1px solid rgba(52, 152, 219, 0.5); color: #3498db; padding: 8px 15px; border-radius: 6px; cursor: pointer; white-space: nowrap;">
                📋 复制
              </button>
            </div>
          </div>

          <div style="background: rgba(52, 152, 219, 0.2); padding: 15px; border-radius: 8px;">
            <p style="color: rgba(255,255,255,0.9); margin-bottom: 8px;">💡 <strong>Level 3 登录信息</strong></p>
            <p style="color: rgba(255,255,255,0.8); margin: 5px 0;">• 用户名：<code style="background: rgba(0,0,0,0.3); padding: 2px 8px; border-radius: 4px;">${data.taskId}</code></p>
            <p style="color: rgba(255,255,255,0.8); margin: 5px 0;">• 密码：<code style="background: rgba(0,0,0,0.3); padding: 2px 8px; border-radius: 4px;">test123</code></p>
            <div style="margin-top: 12px;">
              <a href="/level3/login" style="display: inline-block; background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); color: #fff; padding: 8px 20px; border-radius: 6px; text-decoration: none; font-size: 0.95em;">
                前往 Level 3 →
              </a>
            </div>
          </div>
        </div>
      `;
    }

    // Copy task ID to clipboard
    function copyTaskId(taskId) {
      if (navigator.clipboard) {
        navigator.clipboard.writeText(taskId).then(() => {
          alert('✓ 考试ID已复制到剪贴板');
        }).catch(() => {
          // Fallback for older browsers
          fallbackCopy(taskId);
        });
      } else {
        fallbackCopy(taskId);
      }
    }

    // Fallback copy method
    function fallbackCopy(text) {
      const textArea = document.createElement('textarea');
      textArea.value = text;
      textArea.style.position = 'fixed';
      textArea.style.left = '-999999px';
      document.body.appendChild(textArea);
      textArea.select();
      try {
        document.execCommand('copy');
        alert('✓ 考试ID已复制到剪贴板');
      } catch (err) {
        alert('复制失败，请手动复制：' + text);
      }
      document.body.removeChild(textArea);
    }

    // Clear registration data
    function clearRegistration() {
      if (confirm('确定要清除当前注册信息并重新注册吗？')) {
        localStorage.removeItem('examData');
        document.getElementById('registrationStatus').style.display = 'none';
        document.getElementById('registrationForm').style.display = 'block';
        document.getElementById('registrationResult').style.display = 'none';
        document.getElementById('candidateName').value = '';
      }
    }

    // Register candidate
    async function registerCandidate() {
      const nameInput = document.getElementById('candidateName');
      const resultDiv = document.getElementById('registrationResult');
      const name = nameInput.value.trim();

      if (!name) {
        resultDiv.style.display = 'block';
        resultDiv.style.background = 'rgba(231, 76, 60, 0.2)';
        resultDiv.style.border = '1px solid rgba(231, 76, 60, 0.5)';
        resultDiv.innerHTML = '请输入你的姓名';
        return;
      }

      if (name.length < 2 || name.length > 20) {
        resultDiv.style.display = 'block';
        resultDiv.style.background = 'rgba(231, 76, 60, 0.2)';
        resultDiv.style.border = '1px solid rgba(231, 76, 60, 0.5)';
        resultDiv.innerHTML = '姓名长度必须在2-20个字符之间';
        return;
      }

      try {
        const response = await fetch('/api/register', {
          method: 'POST',
          headers: {
            'Content-Type': 'application/json'
          },
          body: JSON.stringify({ name })
        });

        const data = await response.json();

        if (data.success) {
          // Save to localStorage
          const examData = {
            taskId: data.task_id,
            name: data.name,
            registeredAt: data.registered_at,
            isNew: data.is_new
          };
          localStorage.setItem('examData', JSON.stringify(examData));

          // Show registered status
          showRegisteredStatus(examData);
        } else {
          resultDiv.style.display = 'block';
          resultDiv.style.background = 'rgba(231, 76, 60, 0.2)';
          resultDiv.style.border = '1px solid rgba(231, 76, 60, 0.5)';
          resultDiv.innerHTML = `错误: ${data.error}`;
        }
      } catch (error) {
        resultDiv.style.display = 'block';
        resultDiv.style.background = 'rgba(231, 76, 60, 0.2)';
        resultDiv.style.border = '1px solid rgba(231, 76, 60, 0.5)';
        resultDiv.innerHTML = `网络错误: ${error.message}`;
      }
    }

    // Allow Enter key to submit
    document.addEventListener('DOMContentLoaded', function() {
      const nameInput = document.getElementById('candidateName');
      if (nameInput) {
        nameInput.addEventListener('keypress', function(e) {
          if (e.key === 'Enter') {
            registerCandidate();
          }
        });
      }
    });
  </script>
</body>
</html>
